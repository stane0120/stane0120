button type: "button"; 으로 수정
</label>을 뒤로 보내 input을 감싼다 or for ="" 속성 추가
name= "같은 값"

첫번째로 선택되게 한다면 <option selected>

<form action='insertMember.do" method="POST">

input(name(제출 시 key), value(제출 시 value) placeholder)

text 속성 : size(인풋태그 전체의 크기)']{
, maxlength(글자를 쓸 수 있는 개수)
, email, tel, search, url


checked 속성
checkbox, radio

selected 속성
option

required 속성
모든 인풋

textarea(rows, cols 속성)

-> 여러 줄 작성 가능한 input type="text"


action 속성
제출할 url(주소)

method 속성(get/post)
제출 방식 GET : 주소에 name=value 형식을 값을 담아 제출
POST 주소 X (HTTP Body에 담아 숨겨서 제출)


a (href, target, 점프)
a href (주소로 연결)